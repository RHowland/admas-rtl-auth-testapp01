const AuthConfig = {
  // For best results, ensure Max_Inactive_Seconds is divisible by 60 (e.g., 120 % 60 = 0). Make sure these two fields might be number.
  Max_Inactive_Seconds: 3600,
  Waiting_Seconds: 60,
};

export default AuthConfig